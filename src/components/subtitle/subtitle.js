import React from 'react';
import './subtitle.css'

const Subtitle = ( { form } ) => {
  return <span>Please, type <span className="bold">{ form }</span> standard form</span>;
}

export default Subtitle;
